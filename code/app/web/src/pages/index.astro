---
import PostList from '@/component/PostList.astro'
import ProjectsSection from '@/component/ProjectsSection.astro'
import Layout from '@/layout/Layout.astro'
import { getHomepageWriting } from '@/lib/post'
import { siteTagline } from '@shared/lib/site'

const { latestPosts } = await getHomepageWriting()
---

<Layout>
  <div class="wrap">
    <section class="hero">
      <h1 class="lead hero__lead">{siteTagline}</h1>
      <p class="hero__cta">
        <a class="text-link" href="/about">About →</a>
        <a class="text-link" href="/writing">Writing →</a>
      </p>
    </section>

    <ProjectsSection />

    {
      latestPosts.length > 0 && (
        <section class="home-section">
          <div class="home-section__head">
            <h2>Latest writing</h2>
            <a class="text-link" href="/writing">
              All writing →
            </a>
          </div>
          <PostList posts={latestPosts} />
        </section>
      )
    }
  </div>
</Layout>
